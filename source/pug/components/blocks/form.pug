mixin form()
  section.form.container
    form(action="#" method="post")
      +payment()
      h2 Заполните контактные данные
      input(type="text" name="name" id="name" placeholder="Имя" required)
      label(for="name")
      input(type="tel" name="tel" id="tel" placeholder="Телефон" pattern="[\+]\d{1}\s[\(]\d{3}[\)]\s\d{3}[\-]\d{2}[\-]\d{2}" minlength="18" maxlength="18" required)
      label(for="tel")
      input(type="email" name="email" id="email" placeholder="Электронная почта" required maxlength="20" )
      label(for="email")
      button(type="submit") Отправить
      small
        | Нажимая на кнопку, я соглашаюсь на
        a(href="#")  обработку персональных данных
        |  с
        a(href="#")  правилами пользования Платформой
